name: Build and Release

on:
  push:
    # 仅在 master 分支有推送时触发
    branches: [ master, v2 ]
    # 在创建形如 v*.*.* 的 tag 时触发 (用于自动发布 Release)
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        ref: ${{ github.ref }}

    - name: Setup Python and uv
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'
    - run: python -m pip install --upgrade pip uv
      shell: powershell

    - name: Install Dependencies
      run: uv sync
      shell: powershell

    - name: Update Version File and Get Version
      id: get_version
      run: |
        pip install toml
        $version = python -c "import toml; print(toml.load('pyproject.toml')['project']['version'])"
        Write-Host "成功读取版本号: $version"

        $version_file = "src/bililive_utility/utils/version.py"
        $version_content = "__version__ = `"$version`""
        Set-Content -Path $version_file -Value $version_content -Encoding utf8
        Write-Host "已将版本号更新到 $version_file"

        echo "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
      shell: powershell

    - name: Build Application with PyInstaller
      run: |
        uv pip install -e .

        uv run pyinstaller `
            --name "BiliLive Utility" `
            --windowed `
            --noconfirm `
            --clean `
            --add-data "static;static" `
            --hidden-import "uvicorn.logging" `
            --hidden-import "uvicorn.loops" `
            --hidden-import "uvicorn.loops.auto" `
            --hidden-import "uvicorn.protocols" `
            --hidden-import "uvicorn.protocols.http" `
            --hidden-import "uvicorn.protocols.http.auto" `
            --hidden-import "uvicorn.protocols.websockets" `
            --hidden-import "uvicorn.protocols.websockets.auto" `
            --hidden-import "uvicorn.lifespan" `
            --hidden-import "uvicorn.lifespan.on" `
            --icon="static/favicon.ico" `
            src/bililive_utility/__main__.py
      shell: powershell

    - name: Upload Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: BiliLive-Utility-v${{ steps.get_version.outputs.version }}
        path: dist/
